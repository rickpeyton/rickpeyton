version: '3'
services:
  jekyll:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace
      - ~/.gitconfig:/root/.gitconfig
      - /var/run/docker.sock:/var/run/docker.sock
      - gems:/usr/local/bundle:delegated
      - vscode:/root/.vscode-server:delegated
    command: sleep infinity
    ports:
      - '4000:4000'
volumes:
  gems:
  vscode:
